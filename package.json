{
  "name": "puhelinluettelo_be",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node index.js",
    "dev": "node --watch index.js",
    "deploy:full": "cd ../puhelinluettelo_fe && npm run build:ui && cd ../puhelinluettelo_be && git add . && git commit -m uibuild && git push -u origin main"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "express": "^4.21.2",
    "morgan": "^1.10.0",
    "react-router": "^7.4.1",
    "react-router-dom": "^7.4.1"
  }
}
